================================================================================
                                样例使用说明
================================================================================
版本历史
日期        版本    负责人     IAR     MDK     GCC     描述
2020-06-12  1.0     Heqb     7.70    5.16    8.3.1   First version
================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。

================================================================================
功能描述
================================================================================
本样例主要展示TMR0的捕获功能，在捕获中断中将捕获的计数值读出并保存。

说明：
使能TMR0输入捕获中断功能，根据需要在main.c 90行选择时钟源,时钟分频和计数匹配值,
同时使能EIRQ0下降沿和上升沿产生内部触发事件，通过事件触发TMR0输入捕获功能，进入TMR0中断。


================================================================================
测试环境
================================================================================
测试用板:
---------------------
EV_F4A0_LQ176_V10

辅助工具:
---------------------
无

辅助软件:
---------------------
无

================================================================================
使用步骤
================================================================================
1） 打开工程编译并全速运行。
2） 短按按键SW10，产生EIRQ0的下降沿和上升沿。
3） LED蓝灯常亮，表示进入两次输入捕获中断，并且采集捕获值完毕。

================================================================================
注意
================================================================================
在异步时钟下，必须保证所选的异步时钟开启。否则TMR0将不会计数，配置也不一定准确。
异步时钟下，对TMR0寄存器操作需要3个异步时钟才能写入。

================================================================================
