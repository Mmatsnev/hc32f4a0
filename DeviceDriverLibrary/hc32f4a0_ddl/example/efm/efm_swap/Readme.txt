================================================================================
                                样例使用说明
================================================================================
版本历史
日期        版本    负责人     IAR     MDK     GCC     描述
2020-06-12  1.0     Heqb     7.70    5.16    8.3.1   First version
================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。

================================================================================
功能描述
================================================================================
本样例展示EFM引导交换功能。程序先下载到flash0的起始地址，使能引导交换功能后，
下一次程序将下载到flash1的起始地址。关闭引导交换功能后，下一次程序将下载至flash0
的起始地址。如此可实现引导交换功能。

================================================================================
测试环境
================================================================================
测试用板:
---------------------
EV_F4A0_LQ176_V10

辅助工具:
---------------------
无

辅助软件:
---------------------
无

================================================================================
使用步骤
================================================================================
1）打开工程并重新编译；
2）启动IDE的下载和调试功能，全速运行，可观察到LED_B（蓝灯）点亮(引导交换未使能)；
3）按下按键SW1，使能引导交换功能；
4）复位MCU，重新下载程序，全速运行，可观察到LED_R(红灯)点亮；
5）按下按键SW2，关闭引导交换功能；
6）复位MCU，全速运行，可观察到LED_B(蓝灯)点亮；

================================================================================
注意
================================================================================
1.OTP功能开启，flash0和flash1的前128K区域不会交换，复位MCU，LED_B和LED_R不会交替闪烁
2.在bus_hold时，进行编程和擦除请勿单步调试，单步调试有可能使芯片发生未知错误
================================================================================
