================================================================================
                                样例使用说明
================================================================================
版本历史
日期        版本    负责人     IAR     MDK     GCC     描述
2020-06-12  1.0     Hongjh     7.70    5.16    8.3.1   First version

================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。

================================================================================
功能描述
================================================================================
本样例主要展示如何使用Timer4专用比较功能——延迟触发模式。

说明：
Timer4专用比较功能比较触发模式：计数器单元CNT与OCO比较匹配后，经过SEVT延迟后，产
生专用比较信号时间，该事件启动ADC采样。

================================================================================
测试环境
================================================================================
测试用板:
---------------------
EV_F4A0_LQ176_V10

辅助工具:
---------------------
示波器Tektronix DPO2024B

辅助软件:
---------------------
串口助手软件

================================================================================
使用步骤
================================================================================
1）测试板J2-PD12(TIM4_<t>_ADSM)引脚与示波器连接，将J13拨码开关调为VEXT<->VR；
2）打开串口助手软件，配置端口如下参数：
     波特率：115200
     数据位：8
     校验位：None
     停止位：1
3）通过USB数据线，连接PC和板子J25 DAP，编译、下载并运行；
4）观察示波器，示波器显示周期脉冲（4Hz），占空比50%，触发ADC采样的周期间隔；
5）调改可调电阻R105；
6）通过串口助手，查看m_au16AdcVal值相应跟随改变；
7) 重复步骤5） ~步骤6）。

================================================================================
注意
================================================================================
无

================================================================================
