================================================================================
                                样例使用说明
================================================================================
版本历史
日期        版本    负责人     IAR     MDK     GCC     描述
2020-06-12  1.0     Wuze     7.70    5.16    8.3.1   First version

================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。

================================================================================
功能描述
================================================================================
本例程以ADC的单元1（M4_ADC1）的序列A和DAM单元1（M4_DMA1）的通道0为例，实现了通过DMA读取
ADC数据。例程实现原理：PE7的下降沿触发ADC的序列A扫描，序列A扫描结束后，产生事件“EVT_ADC1_EOCA”，
该事件触发DMA的通道0从指定的ADC数据寄存器地址（源地址）读取指定长度的数据到指定的目标地址，
如此重复。

说明：
本例程中，DAM的块大小（block size）必须覆盖从最小通道编号到最大通道编号的所有同道，因为DMA
每读取一个数据后，数据指针加1，指向下一个需要读取的数据地址。程中选用的ADC通道为ADC_CH2、
ADC_CH3和ADC_CH5，那么DMA的块大小须设置为不小于4，同样地，用于保存ADC数据的数组的大小，也
应不小于4。如果想要DMA只读取使用的通道的数据，可将这些通道对应的模拟输入引脚重新映射到连续
的ADC通道，关于通道重映射，详见例程“adc_09_channel_remap”。

================================================================================
测试环境
================================================================================
测试用板:
---------------------
EV_F4A0_LQ176_V10

辅助工具:
---------------------
USB转串口工具

辅助软件:
---------------------
串口调试助手软件（115200bps，8N1）

================================================================================
使用步骤
================================================================================
1）将电位器旁的J13开关拨为VEXT<->VR；
2）如果需要用在上位机观察程序运行情况，请将USB转串口工具正确地与开发板上的J28端口（靠近
   MicroUSB接口）连接；如果不需要，请忽略此步骤；
3）打开工程并重新编译；
4）启动IDE的下载和调试功能；
5）全速运行程序或在需要的位置设置断点；
6）在PE7上制造一个下降沿，触发序列A扫描。

================================================================================
注意
================================================================================
无

================================================================================
